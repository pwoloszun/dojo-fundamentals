/*
 * Diablo:
 data: Player (strength, agility, inteligence, level)
 roles: Warrior, Rogue, Sorcerer, ExperiencePointsDistributor(increment("strength"))
 Role interface: hit(target), spell(target), shoot(target), levelUp() - % zwieksza statsy zaleznie od klasy bohatera
 contexts: FightContext, experiencePointsDistributionContext
 * */

define([
], function() {
  return {
    run: function() {
    }
  };
});
