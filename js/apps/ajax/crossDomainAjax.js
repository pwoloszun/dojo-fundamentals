define([
], function() {
  return {
    run: function() {
      // pobierz JSON z danymi userow i wyrenderuj

      // pobierz dane z githuba i wyrenderuj
      var githubUrl = "https://api.github.com/users/pwoloszun";

      // pobierz dane z yahoo i wyrenderuj
      var yahooUrl = 'http://query.yahooapis.com/v1/public/yql?q=select * from geo.places where text="sunnyvale, ca"';
    }
  };
});
