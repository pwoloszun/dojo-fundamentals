/*
* JS fiddle:
* 1. mamy 3 obiekty: jsSrc, htmlSrc, cssSrc, kazdy ma metode update(params), getContent()
* 2. aktualizacja w ktorym kolwiek z ww. obiektow, powoduje powiadomienie
*   o aktualizacji obiektow:
*   - resultApp.build(jsContent, htmlContent, cssContent)
 *  - oraz consoleLog.build(jsContent, htmlContent, cssContent)
* */
define([
], function() {
  return {
    run: function() {
    }
  };
});
