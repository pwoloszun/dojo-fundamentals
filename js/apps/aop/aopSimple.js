/*
* 1. Stworz klase Cart
*   + addItem(item)
* 2. w aplikacji dodaj advice'y:
*   + before: przeprowadzi validacje obiektu item
*   + after: wyrenderuje obiekt cart na widoku cartView
*   + around: opakuje operacje addItem() w transakcje
* */

define([
], function(aspect, Cart) {
  return {
    run: function() {
    }
  };
});
