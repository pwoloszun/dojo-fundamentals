/*
  1. Stworz hierarchie klas: Human <- Hero <- SuperHero
  2. dla kazdej klasy zaimplementuj metode log(), przetestuj wyniki
  3. w definicji klasy Hero dodaj lancuchowanie: "-chains-": {
        getName: "before"
      }, przetestuj
  4. cofnij zmiany z 3. i wprowadz je dla klasy SuperHero
* */

define([
], function() {
  return {
    run: function() {
    }
  };
});
