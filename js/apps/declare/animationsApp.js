/* Inheritance:
 1. klasa bazowa Animation
    + constructor($el)
    + progress(fn(time, pctComplete))
    + onStart(fn), onEnd(fn), onMouseEnter(fn))
 2. klasy dziedziczace: FlashAnim, JSAnim, CanvasAnim
    + start()
    + pause()
* */
