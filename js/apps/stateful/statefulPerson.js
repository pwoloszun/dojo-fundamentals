/*
  1. Zdefiniuj klase Person, z dwoma property: firstName, lastName, dziedziczaca po Stateful
  2. w aplikacji obserwuj:
    - zmiany dowolnego atrybutu
    - zmiane atrybutu lastName
* */

define([
], function(Person) {
  return {
    run: function() {
    }
  };
});
